events {}
http {
    server {
        listen 80;
        listen [::]:80;
        server_name example.com;

        root        /www;
        index       index.html;
        charset     utf-8;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
dist: trusty
language: c
group: deprecated-2017Q4
sudo: required
services:
  - docker
git:
    depth: 1
git:
  submodules: false

matrix:
  include:
    # works on Precise and Trusty
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - build-essential
            - gcc-multilib
            - g++-multilib

before_install:
    - git submodule update --init --recursive --remote .

script:
    - cd docker/naclruntime
    - docker build -t alyptik/lind:latest .
    - docker run --rm --ipc=host --cap-add=SYS_PTRACE -it alyptik/lind:naclruntime /home/lind/lind_project/lind/repy/bin/lind /bin/bash -c 'a=potato; [[ $a =~ pota.* ]] && echo foo; echo bar' || true

# vi:ft=yaml:
